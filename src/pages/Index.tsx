const Index = () => {
  return (
    <main className="min-h-screen p-8 flex items-center justify-center">
      <section className="game-panel p-8 text-center max-w-xl">
        <h1 className="text-4xl font-bold mb-3">Lovable Starter Imported</h1>
        <p className="text-muted-foreground">Your Vite + React + Tailwind baseline is now set up.</p>
      </section>
    </main>
  );
};

export default Index;
